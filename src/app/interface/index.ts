export interface IStudentTable {
  username: string;
  firstName: string;
  lastName: string;
  mInitial: string;
  suffix: string;
  yearLevel: string;
  actions: string;
}

export interface IRoleId {
  roleID: number;
  roleName: string;
}

